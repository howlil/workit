{
  "name": "@workit/worker",
  "version": "0.1.0",
  "description": "Workit Cloudflare Worker API & MCP backend (D1 & Hono)",
  "private": true,
  "type": "module",
  "author": "howlil",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/howlil/workit.git",
    "directory": "apps/worker"
  },
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.7.11",
    "@workit/domain": "workspace:*",
    "@workit/contracts": "workspace:*",
    "@workit/db": "workspace:*",
    "@workit/shared": "workspace:*",
    "@workit/mcp": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^4.18.0",
    "@cloudflare/workers-types": "^5.20260921.1",
    "typescript": "catalog:"
  }
}
